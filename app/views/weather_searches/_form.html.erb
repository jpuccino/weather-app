<%= form_with(model: weather_search) do |form| %>
  <% if weather_search.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(weather_search.errors.count, "error") %> prohibited this weather_search from being saved:</h2>

      <ul>
        <% weather_search.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :city, style: "display: block" %>
    <%= form.text_field :city %>
  </div>

  <div>
    <%= form.label :state, style: "display: block" %>
    <%= form.text_field :state %>
  </div>

  <div>
    <%= form.label :country, style: "display: block" %>
    <%= form.text_field :country %>
  </div>

  <div>
    <%= form.label :zip, style: "display: block" %>
    <%= form.number_field :zip %>
  </div>

  <div>
    <%= form.submit "Show me the weather" %>
  </div>
<% end %>
